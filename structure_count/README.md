# structure_count

诊断探针（`use=probe`），**不要放进默认训练集**。中文奥数风格线稿计数，答案由组合公式给出。

- `split=test`：冻结的 10 条，禁止训练（参数不得出现在 train）
- `split=train`：240 条，机制相同、参数不同；仍不是推荐 SFT
- 默认 `image` 为 720px；`image_low` 为同一 SVG 的 144px

一次历史小模型探索未在这 10 条上观察到稳定学习，但完整训练与评测产物未保留；该观察不可独立复核，也不是对组合计数能力的一般性结论。
