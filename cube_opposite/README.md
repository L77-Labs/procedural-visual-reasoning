# cube_opposite

诊断子集，**不要放进默认训练集**（`use=diagnostic`）。

与 `cube_fold` 同一批 900 场景，问展开图折叠后某颜色的对面。几何标签可由程序重算；教师解释来自阿里云百炼正式 API 的 `qwen3.8-max`，无「选A」泄漏，但未逐句程序核验。

历史探索中曾观察到对外部空间代理任务的负迁移，但完整 checkpoint、逐题预测和冻结评测集未保留，结果不可独立复核。它用于提出「同类题未必迁移」这一待验证问题，不是该命题的最终证据，也不要当默认空间 SFT。
