# Data Card

## 概览

本仓库是一份中文程序化视觉推理工具包。它提供场景参数、渲染图、问题、答案、解释、生成器和验证器，适合小规模 SFT、机制诊断和数据工程研究；它不是通用视觉推理基准。

## 子集与默认用途

| 子集 | use | 默认训练 | 标签来源 |
|---|---|---:|---|
| `cube_fold` | `sft` | 是 | 折叠几何 |
| `vessels` | `sft` | 是 | 简化物理模拟 |
| `cube_opposite` | `diagnostic` | 否 | 折叠几何 |
| `structure_count` | `probe` | 否 | 组合公式 |
| `leak_contrast` | `contrast` | 否 | 折叠几何；教师解释故意保留答案泄漏 |

`load_sft()` 默认只加载 `cube_fold` 和 `vessels` 的 `split=train`。`structure_count` 中 `split=test` 的 10 条记录禁止训练。

## 可验证范围

- 标签可由几何、物理模拟或组合公式重新计算。
- `cube_fold` 与 `vessels` 的默认 `rationale` 是程序化解释，可由当前规则重新生成。
- 教师解释只核对来源、格式和泄漏，不保证逐句事实正确。
- test 主要是同一生成器的场景留出，不代表自然图像或通用推理分布。`vessels` 的 train 和 test 都含 5/6 容器，不能称为容器数量 OOD。

## 教师解释来源

部分解释由阿里云百炼正式 API 的 `qwen3.8-max` 生成：

- `vessels`：327 条原始教师解释保存在 `rationale_teacher`，默认 `rationale` 仍为物理程序生成；
- `cube_opposite`：900 条默认解释；
- `leak_contrast`：300 条默认解释，其中 298 条被当前规则检出答案字母泄漏。

这些文本未逐句程序核验。数据集级来源适用于未携带逐条 provider 字段的既有记录；新导出记录会使用 `teacher_model`、`teacher_provider` 和 `teacher_verified` 字段。

## 证据等级

| 主张 | 状态 |
|---|---|
| 标签、图片存在性、split 隔离和默认加载量 | `verified`，可由当前仓库检查 |
| clean-300 可由历史 900 条数据按 seed 复原 | `verified`，但不属于当前默认可见三面 SFT |
| 历史实验观察到对外部空间代理任务的负迁移 | `recorded`，完整产物未保留，不能独立复核 |
| 视觉 LoRA 普遍无效或这些数据能提升通用能力 | `unsupported`，本仓库不作此结论 |

## 已知限制

- 可见三面只对应图中的固定轴测朝向。
- 连通器采用离散事件模型：水位达到管道阈值时触发瞬时连通，并以矩形截面计算水量和平衡状态。该定义用于获得可确定重算的标签，不用于近似真实流体动力学。
- 5–6 容器图可能出现标签贴墙或叠字。
- 程序化 test 与 train 共享渲染和语言风格，不能代替外部基准。
- 教师解释可能包含事实错误、表达偏差或答案泄漏。

## 许可

生成器代码采用 MIT。图、题、答案、场景参数和解释按 `LICENSE-DATA` 中的 CC BY 4.0 发布。部分教师解释的生成来源如上所述；使用者仍需根据具体用途自行判断合规性。
